name: tests

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  unit-tests:
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest]
        python-version: ["3.10", "3.11", "3.12"]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
          cache: pip
      - name: Install core and development dependencies
        run: python -m pip install -e ".[dev]"
      - name: Check CLI installation
        run: interview-recap --help
      - name: Compile modules
        run: python -m py_compile src/*.py
      - name: Lint
        run: python -m ruff check src tests
      - name: Run unit tests
        run: python -m unittest discover -s tests -v

  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip
      - name: Install build frontend
        run: python -m pip install build
      - name: Build wheel and source distribution
        run: python -m build
      - name: Install wheel and run offline analysis smoke test
        run: |
          python -m pip install dist/*.whl
          repository_root="$PWD"
          cd "$(mktemp -d)"
          interview-recap --version
          interview-recap analyze "$repository_root/examples/synthetic_interview.v2.json" --provider none --output-dir /tmp/interview-recap-smoke
